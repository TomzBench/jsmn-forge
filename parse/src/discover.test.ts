import { describe, it, expect } from "vitest";

describe("discover", () => {
  it("should detected valid config", () => {});
  it("should support different config file name", () => {
    expect(1).toBe(1);
  });
});
